printf("%d ROW", row);

        // printf("Check State array:\n");
        // for (int i = 0; i < sizeof(checkState) / sizeof(checkState[0]); ++i)
        // {
        //     printf("%d ", checkState[i]);
        // }
        // printf("\n");